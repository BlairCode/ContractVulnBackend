# ================= 核心依赖 =================
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-CORS==4.0.0
Werkzeug==2.3.7

# ================= 机器学习相关 =================
torch>=1.9.0
torchvision>=0.10.0
numpy>=1.21.0
scikit-learn>=1.0.0

# ================= HTTP请求 =================
requests>=2.28.0

# ================= PDF生成 =================
weasyprint>=59.0
pdfkit>=1.0.0
# 注意：pdfkit需要系统安装wkhtmltopdf

# ================= 数据库 =================
# SQLite (Python内置，无需安装)
# PostgreSQL支持（可选）
# psycopg2-binary>=2.9.0

# ================= 开发工具 =================
python-dotenv>=0.19.0

# ================= 系统依赖说明 =================
# 如果使用 weasyprint，可能需要安装系统依赖：
# Ubuntu/Debian: sudo apt-get install build-essential python3-dev python3-pip python3-setuptools python3-wheel python3-cffi libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev shared-mime-info
# macOS: brew install cairo pango gdk-pixbuf libffi
# CentOS/RHEL: sudo yum install redhat-rpm-config python3-devel python3-pip python3-setuptools python3-wheel python3-cffi libffi-devel cairo pango gdk-pixbuf2

# ================= 版本兼容性 =================
# Python >= 3.8

